# Konfigurasi environment lokal — salin dari .env.example, jangan commit.
.env

# Tool lokal (Claude, Kilo) — konfig di folder ini pribadi, jangan commit.
.claude
.kilo

# Python
**/__pycache__/
*.pyc
autonomy/.venv/

# Config tuning lokal (hasil tuning kolam tim) — jangan commit, cukup example*
autonomy/config/*.local.yaml
autonomy/config/*.local.yml
autonomy/config/*.local.json

# Dataset simulasi bawah air (hasil tests/evaluate_qr_underwater.py) — regenerate
# saja dari seed; jangan commit gambarnya.
autonomy/dataset/
dataset/
autonomy/calib_imgs_trial2
autonomy/calib_imgs_trial2_dense
# ZIP dinamai dari --outdir (mis. dataset/ -> dataset.zip)
autonomy/dataset.zip
dataset.zip
.claude

# Editor / OS
*.swp
*.swo
*~

# Model 3D besar
public/models/rov.obj

# Planning
Planning/ANALISIS-PID-ROLL-PITCH.md
server/GUI-ROV.log
ros2_ws.code-workspace
