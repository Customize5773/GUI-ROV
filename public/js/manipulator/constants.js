export const GripState = Object.freeze({
    OPENING: "OPENING",
    CLOSING: "CLOSING",
    STOP: "STOP"
});

export const RotateState = Object.freeze({
    LEFT: "LEFT",
    RIGHT: "RIGHT",
    STOP: "STOP"
});

export const HardwareType = Object.freeze({
    SERVO: "SERVO",
    BLDC: "BLDC"
});

export const ManipulatorAction = Object.freeze({
    START: "start",
    STOP: "stop"
});

export const GripDirection = Object.freeze({
    OPEN: "open",
    CLOSE: "close"
});

export const RotateDirection = Object.freeze({
    LEFT: "left",
    RIGHT: "right"
});